// for loop accepts a single statement as its body.

for (let i = 0; i < 10; i++) console.log(i);

// If you want to use more than one statement in the loop body,you can group them into a block statement.

for (let j = 1; j < 5; j++) {
	console.log(j);
	console.log(j * 2);
}
